{
  "name": "agendamentobarbeariaflutter",
  "version": "1.0.0",
  "description": "Código referente ao sistema de agendamento em flutter de uma barbearia",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "api": "cd api && node server.js",
    "flutter": "flutter run -d chrome",
    "dev": "concurrently \"npm run api\" \"npm run flutter\"",
    "start": "npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
